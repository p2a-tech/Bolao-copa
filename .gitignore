node_modules
.next
out
build
*.log
.env
.env*.local
prisma/*.db
prisma/*.db-journal
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo
.claude/
# Hero images dos jogadores (public/teams/*.png) AGORA SÃO COMMITADAS no repo
# para funcionarem em deploys serverless (Vercel/Netlify) onde o filesystem
# não persiste. Regenere apenas se quiser substituir uma imagem específica:
#   npm run images:hero[:force]
# uploads de usuários (logos de patrocinadores etc) — específicos por instalação
/public/uploads/
