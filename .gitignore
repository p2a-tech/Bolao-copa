node_modules
.next
out
build
*.log
.env
.env*.local
prisma/*.db
prisma/*.db-journal
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo
.claude/
# hero images geradas (gpt-image-2) — pesadas; reusar via npm run images:hero
/public/teams/
# uploads de usuários (logos de patrocinadores etc) — específicos por instalação
/public/uploads/
